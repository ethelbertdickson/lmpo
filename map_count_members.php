<?php 
//	Define the query to determine the total number of  registered users:
$q = "SELECT first_name, last_name FROM users WHERE state_id = 1";
$r = @mysqli_query ($dbc, $q); // Run the query.
//Count the number of returned rows:
$num1 = mysqli_num_rows($r);
if ($num1 > 0) { 

}

//	Define the query to determine the total number of  registered users:
$q = "SELECT first_name, last_name FROM users WHERE state_id = 2";
$r = @mysqli_query ($dbc, $q); // Run the query.
//Count the number of returned rows:
$num2 = mysqli_num_rows($r);
if ($num2 > 0) { 

}

//	Define the query to determine the total number of  registered users:
$q = "SELECT first_name, last_name FROM users WHERE state_id = 3";
$r = @mysqli_query ($dbc, $q); // Run the query.
//Count the number of returned rows:
$num3 = mysqli_num_rows($r);
if ($num3 > 0) { 

}

//	Define the query to determine the total number of  registered users:
$q = "SELECT first_name, last_name FROM users WHERE state_id = 4";
$r = @mysqli_query ($dbc, $q); // Run the query.
//Count the number of returned rows:
$num4 = mysqli_num_rows($r);
if ($num4 > 0) { 

}

//	Define the query to determine the total number of  registered users:
$q = "SELECT first_name, last_name FROM users WHERE state_id = 5";
$r = @mysqli_query ($dbc, $q); // Run the query.
//Count the number of returned rows:
$num5 = mysqli_num_rows($r);
if ($num5 > 0) { 

}

//	Define the query to determine the total number of  registered users:
$q = "SELECT first_name, last_name FROM users WHERE state_id = 6";
$r = @mysqli_query ($dbc, $q); // Run the query.
//Count the number of returned rows:
$num6 = mysqli_num_rows($r);
if ($num6 > 0) { 

}

//	Define the query to determine the total number of  registered users:
$q = "SELECT first_name, last_name FROM users WHERE state_id = 7";
$r = @mysqli_query ($dbc, $q); // Run the query.
//Count the number of returned rows:
$num7 = mysqli_num_rows($r);
if ($num7 > 0) { 

}

//	Define the query to determine the total number of  registered users:
$q = "SELECT first_name, last_name FROM users WHERE state_id = 8";
$r = @mysqli_query ($dbc, $q); // Run the query.
//Count the number of returned rows:
$num8 = mysqli_num_rows($r);
if ($num8 > 0) { 

}

//	Define the query to determine the total number of  registered users:
$q = "SELECT first_name, last_name FROM users WHERE state_id = 9";
$r = @mysqli_query ($dbc, $q); // Run the query.
//Count the number of returned rows:
$num9 = mysqli_num_rows($r);
if ($num9 > 0) { 

}

//	Define the query to determine the total number of  registered users:
$q = "SELECT first_name, last_name FROM users WHERE state_id = 10";
$r = @mysqli_query ($dbc, $q); // Run the query.
//Count the number of returned rows:
$num10= mysqli_num_rows($r);
if ($num10> 0) { 

    
}


//	Define the query to determine the total number of  registered users:
$q = "SELECT first_name, last_name FROM users WHERE state_id = 11";
$r = @mysqli_query ($dbc, $q); // Run the query.
//Count the number of returned rows:
$num11 = mysqli_num_rows($r);
if ($num11 > 0) { 

}

//	Define the query to determine the total number of  registered users:
$q = "SELECT first_name, last_name FROM users WHERE state_id = 12";
$r = @mysqli_query ($dbc, $q); // Run the query.
//Count the number of returned rows:
$num12 = mysqli_num_rows($r);
if ($num12 > 0) { 

}

//	Define the query to determine the total number of  registered users:
$q = "SELECT first_name, last_name FROM users WHERE state_id = 13";
$r = @mysqli_query ($dbc, $q); // Run the query.
//Count the number of returned rows:
$num13 = mysqli_num_rows($r);
if ($num13 > 0) { 

}

//	Define the query to determine the total number of  registered users:
$q = "SELECT first_name, last_name FROM users WHERE state_id = 14";
$r = @mysqli_query ($dbc, $q); // Run the query.
//Count the number of returned rows:
$num14 = mysqli_num_rows($r);
if ($num14 > 0) { 

}

//	Define the query to determine the total number of  registered users:
$q = "SELECT first_name, last_name FROM users WHERE state_id = 15";
$r = @mysqli_query ($dbc, $q); // Run the query.
//Count the number of returned rows:
$num15 = mysqli_num_rows($r);
if ($num15 > 0) { 

}

//	Define the query to determine the total number of  registered users:
$q = "SELECT first_name, last_name FROM users WHERE state_id = 16";
$r = @mysqli_query ($dbc, $q); // Run the query.
//Count the number of returned rows:
$num16 = mysqli_num_rows($r);
if ($num16 > 0) { 

}

//	Define the query to determine the total number of  registered users:
$q = "SELECT first_name, last_name FROM users WHERE state_id = 17";
$r = @mysqli_query ($dbc, $q); // Run the query.
//Count the number of returned rows:
$num17 = mysqli_num_rows($r);
if ($num17 > 0) { 

}

//	Define the query to determine the total number of  registered users:
$q = "SELECT first_name, last_name FROM users WHERE state_id = 18";
$r = @mysqli_query ($dbc, $q); // Run the query.
//Count the number of returned rows:
$num18 = mysqli_num_rows($r);
if ($num18 > 0) { 

}

//	Define the query to determine the total number of  registered users:
$q = "SELECT first_name, last_name FROM users WHERE state_id = 19";
$r = @mysqli_query ($dbc, $q); // Run the query.
//Count the number of returned rows:
$num19 = mysqli_num_rows($r);
if ($num19 > 0) { 

}

//	Define the query to determine the total number of  registered users:
$q = "SELECT first_name, last_name FROM users WHERE state_id = 20";
$r = @mysqli_query ($dbc, $q); // Run the query.
//Count the number of returned rows:
$num20= mysqli_num_rows($r);
if ($num20> 0) { 

    
}


//	Define the query to determine the total number of  registered users:
$q = "SELECT first_name, last_name FROM users WHERE state_id = 21";
$r = @mysqli_query ($dbc, $q); // Run the query.
//Count the number of returned rows:
$num21 = mysqli_num_rows($r);
if ($num21 > 0) { 

}

//	Define the query to determine the total number of  registered users:
$q = "SELECT first_name, last_name FROM users WHERE state_id = 22";
$r = @mysqli_query ($dbc, $q); // Run the query.
//Count the number of returned rows:
$num22 = mysqli_num_rows($r);
if ($num22 > 0) { 

}

//	Define the query to determine the total number of  registered users:
$q = "SELECT first_name, last_name FROM users WHERE state_id = 23";
$r = @mysqli_query ($dbc, $q); // Run the query.
//Count the number of returned rows:
$num23 = mysqli_num_rows($r);
if ($num23 > 0) { 

}

//	Define the query to determine the total number of  registered users:
$q = "SELECT first_name, last_name FROM users WHERE state_id = 24";
$r = @mysqli_query ($dbc, $q); // Run the query.
//Count the number of returned rows:
$num24 = mysqli_num_rows($r);
if ($num24 > 0) { 

}

//	Define the query to determine the total number of  registered users:
$q = "SELECT first_name, last_name FROM users WHERE state_id = 25";
$r = @mysqli_query ($dbc, $q); // Run the query.
//Count the number of returned rows:
$num25 = mysqli_num_rows($r);
if ($num25 > 0) { 

}

//	Define the query to determine the total number of  registered users:
$q = "SELECT first_name, last_name FROM users WHERE state_id = 26";
$r = @mysqli_query ($dbc, $q); // Run the query.
//Count the number of returned rows:
$num26 = mysqli_num_rows($r);
if ($num26 > 0) { 

}

//	Define the query to determine the total number of  registered users:
$q = "SELECT first_name, last_name FROM users WHERE state_id = 27";
$r = @mysqli_query ($dbc, $q); // Run the query.
//Count the number of returned rows:
$num27 = mysqli_num_rows($r);
if ($num27 > 0) { 

}

//	Define the query to determine the total number of  registered users:
$q = "SELECT first_name, last_name FROM users WHERE state_id = 28";
$r = @mysqli_query ($dbc, $q); // Run the query.
//Count the number of returned rows:
$num28 = mysqli_num_rows($r);
if ($num28 > 0) { 

}

//	Define the query to determine the total number of  registered users:
$q = "SELECT first_name, last_name FROM users WHERE state_id = 29";
$r = @mysqli_query ($dbc, $q); // Run the query.
//Count the number of returned rows:
$num29 = mysqli_num_rows($r);
if ($num29 > 0) { 

}

//	Define the query to determine the total number of  registered users:
$q = "SELECT first_name, last_name FROM users WHERE state_id = 30";
$r = @mysqli_query ($dbc, $q); // Run the query.
//Count the number of returned rows:
$num30= mysqli_num_rows($r);
if ($num30> 0) { 

    
}


//	Define the query to determine the total number of  registered users:
$q = "SELECT first_name, last_name FROM users WHERE state_id = 31";
$r = @mysqli_query ($dbc, $q); // Run the query.
//Count the number of returned rows:
$num31 = mysqli_num_rows($r);
if ($num31 > 0) { 

}

//	Define the query to determine the total number of  registered users:
$q = "SELECT first_name, last_name FROM users WHERE state_id = 32";
$r = @mysqli_query ($dbc, $q); // Run the query.
//Count the number of returned rows:
$num32 = mysqli_num_rows($r);
if ($num32 > 0) { 

}

//	Define the query to determine the total number of  registered users:
$q = "SELECT first_name, last_name FROM users WHERE state_id = 33";
$r = @mysqli_query ($dbc, $q); // Run the query.
//Count the number of returned rows:
$num33 = mysqli_num_rows($r);
if ($num33 > 0) { 

}

//	Define the query to determine the total number of  registered users:
$q = "SELECT first_name, last_name FROM users WHERE state_id = 34";
$r = @mysqli_query ($dbc, $q); // Run the query.
//Count the number of returned rows:
$num34 = mysqli_num_rows($r);
if ($num34 > 0) { 

}

//	Define the query to determine the total number of  registered users:
$q = "SELECT first_name, last_name FROM users WHERE state_id = 35";
$r = @mysqli_query ($dbc, $q); // Run the query.
//Count the number of returned rows:
$num35 = mysqli_num_rows($r);
if ($num35 > 0) { 

}

//	Define the query to determine the total number of  registered users:
$q = "SELECT first_name, last_name FROM users WHERE state_id = 36";
$r = @mysqli_query ($dbc, $q); // Run the query.
//Count the number of returned rows:
$num36 = mysqli_num_rows($r);
if ($num36 > 0) { 

}

//	Define the query to determine the total number of  registered users:
$q = "SELECT first_name, last_name FROM users WHERE state_id = 37";
$r = @mysqli_query ($dbc, $q); // Run the query.
//Count the number of returned rows:
$num37 = mysqli_num_rows($r);
if ($num37 > 0) { 

}
